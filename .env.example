# Neon Postgres pooled connection string
DATABASE_URL=postgres://user:pass@host/db?sslmode=require
# openssl rand -hex 32
PAYLOAD_SECRET=change-me
# Google Analytics measurement id (optional; GA disabled when empty)
NEXT_PUBLIC_GA_ID=
# Vercel Blob token (optional locally; uploads fall back to ./media)
BLOB_READ_WRITE_TOKEN=
